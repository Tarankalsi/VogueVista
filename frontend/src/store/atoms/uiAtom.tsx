import { atom } from "recoil";

export const openMenuAtom = atom({
    key: "openMenuAtom",
    default: "hidden"
})

export const sizeButtonAtom = atom({
    key: "sizeButtonAtom",
    default: "s"
})

